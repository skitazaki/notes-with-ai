---
type: image
path: /docs/data/security
description: Continuous security operating cycle that sustains data protection across every state of the data lifecycle.
---

# Image Generation Prompt - Security Operating Cycle

Create a publication-quality technical infographic titled exactly:

"Security Operating Cycle"

Output dimensions and format:

- 16:9 aspect ratio
- suitable for display at 1600 × 900 pixels
- clean raster output that can be converted to WebP

Purpose:

Replace the Mermaid diagram in “The Security Operating Cycle: How Protection Is Sustained” on the Data Security hub. Make clear that this is a continuous management loop applied across every state of the data lifecycle—not a sequence of data states.

Composition:

- Arrange six stages clockwise in one continuous circular loop with clear directional arrows.
- Place a compact center label reading exactly:
  - `Sustained data protection`
- Show these stage titles and descriptions exactly, in this clockwise order:
  1. `Understand`
     `inventory, location, lineage`
  2. `Classify & Assess`
     `sensitivity, exposure, threats`
  3. `Protect`
     `encryption, masking, secure handling`
  4. `Control`
     `least privilege, isolation, egress`
  5. `Detect`
     `audit, anomalies, extraction`
  6. `Respond & Recover`
     `contain, investigate, restore, verify`
- Make the return arrow from `Respond & Recover` to `Understand` especially clear and label it exactly `lessons`.
- Add the exact footer caption:
  - `Continuous activities applied across every data lifecycle state.`

Visual hierarchy and style:

- crisp vector-style editorial infographic
- light neutral background with generous whitespace
- restrained navy, teal, muted blue, and soft gray palette
- one warm accent may highlight the `lessons` feedback path
- large, highly legible sans-serif typography
- flat two-dimensional shapes with subtle borders
- balanced, calm, vendor-neutral, and concept-first
- consistent with a modern technical documentation site

Constraints:

- Render every specified label exactly as written.
- Show exactly six stages and one unbroken clockwise loop.
- Preserve the distinction between the operating cycle and the data lifecycle.
- Do not depict the stages as data moving between storage systems.
- Do not add product names, vendor logos, compliance badges, decorative prose, 3D effects, gradients, excessive shadows, or watermarks.
- Do not use a generic padlock, shield hero icon, hacker silhouette, hooded person, binary-code background, server-rack photograph, globe, or cloud-provider logo.
- Avoid tiny text, dense implementation details, and unrelated cybersecurity decoration.
