---
type: image
path: /docs/data/management/reference-data
description: Hero visual showing how enterprise reference data maps to local system values while preserving shared semantics.
---

# Image Generation Prompt - Reference Data across systems

Create a publication-quality technical infographic titled exactly:

"Reference Data across systems"

Output dimensions and format:

- 16:9 aspect ratio
- suitable for display at 1600 × 900 pixels
- clean raster output that can be converted to WebP

Purpose:

Show that Reference Data Management distributes controlled values and mappings across systems. Use the `Status` concept as an unmistakable example: systems may retain different local attribute names and codes while preserving one shared meaning.

Composition:

- Place a compact panel titled "Enterprise Reference Data" at the top center.
- Inside it, show exactly: "Country JP", "Currency JPY", and "Status ACTIVE".
- Render Country and Currency as quiet secondary rows in light gray.
- Give the Status row an accent background and stronger type so it is clearly the mapped attribute.
- From the highlighted Status row, draw direct accent-colored arrows labeled "mappings / distribution" and branch them to three equal panels.
- Label the panels "CRM", "ERP", and "Analytics".
- In each panel, show the local attribute name and value exactly: "Status = Active", "State Code = A", and "is_status = 1", respectively.
- Beneath the panels, add the exact caption "Different attributes and codes, shared meaning: ACTIVE".

Style:

- crisp vector-style editorial infographic
- light background and generous whitespace
- restrained navy, teal, muted blue, and soft gray palette
- large, readable typography
- balanced vertical flow that remains legible at documentation-page width
- vendor-neutral and concept-first

Constraints:

- Render every specified label exactly as written.
- Show precisely three consuming systems.
- Make the arrows visibly originate from the highlighted Status row, not from the enterprise panel in general.
- Make it immediately clear that `Status`, `State Code`, and `is_status` are different local attribute names mapped to the same canonical `Status` concept.
- Make differing local attribute names, codes, and their shared meaning the central idea.
- Do not use an abstract shared-semantics box or an unlabeled chain icon.
- Do not add other explanatory prose, vendor logos, people, databases, 3D effects, gradients, decorative clip art, or watermarks.
