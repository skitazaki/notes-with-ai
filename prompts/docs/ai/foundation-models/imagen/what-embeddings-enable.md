---
type: image
path: /docs/ai/foundation-models/embeddings
description: Conceptual diagram showing the applications enabled by embeddings.
---

# Image Generation Prompt — What Embeddings Enable

Create a polished 1672 × 941 pixels (16:9) technical infographic titled **What Embeddings Enable**.

Place a strong but flat rounded block labeled **Embedding Vector** at the center. Arrange ten compact, evenly weighted capability blocks in two balanced columns, connected directly to the center with thin clean lines: **Semantic Search**, **Recommendation**, **Clustering**, **Classification**, **Deduplication**, **Entity Matching**, **Anomaly Detection**, **Multimodal Retrieval**, **Agent Memory**, and **RAG**. Show a hub-and-spoke relationship: one learned numerical representation supports many applications, but the applications do not form a sequential pipeline.

Use this exact order and icon mapping:

- Left, top to bottom: **Semantic Search** with a magnifying glass; **Recommendation** with a thumbs-up; **Clustering** with a ring of separate dots; **Classification** with a tag; **Deduplication** with two overlapping document sheets.
- Right, top to bottom: **Entity Matching** with one person inside a circle; **Anomaly Detection** with a warning triangle and exclamation mark; **Multimodal Retrieval** with combined image, text, and audio symbols; **Agent Memory** with a brain; **RAG** with a speech bubble.

Use the same color mapping in the English and Japanese versions. Semantic Search and Entity Matching are blue; Recommendation and Anomaly Detection are teal; Clustering and Multimodal Retrieval are indigo; Classification and Agent Memory are cyan-teal; Deduplication and RAG are amber-orange. Each capability block must use its assigned color consistently for the icon, outline, connection node, and connector.

Draw ten independent connectors, one from each capability block directly to the central block. Do not join connectors or use shared trunks. Use thin, straight, symmetric paths with right-angle turns only for the top and bottom rows. Terminate each connector in one small outlined circular node at the capability-block edge.

Match the established Notes with AI illustration style: a solid opaque white background, a large dark-navy title, crisp dark-navy typography, simple outline icons, thin blue, teal, indigo, and amber strokes, subtle pale fills, generous spacing, and almost no shadows. Keep the composition architectural and legible at documentation-column width.

Render every specified label exactly once and exactly as written. Do not add other text, logos, watermarks, product UI, vendor names, humanoid robots, product cards, shopping or search interfaces, document mockups, glossy buttons, heavy shadows, glow, neon, decorative bubbles, confetti, gradients, 3D depth, photorealism, or dense decoration.
