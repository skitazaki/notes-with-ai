+++
date = '2026-05-10T12:40:00+09:00'
title = '認可モデルとポリシーシステム'
weight = 4
prev = '/docs/acc/human-identity'
next = '/docs/acc/workload-identity'
+++

認可は、認証済みの主体が、特定の文脈において、特定のリソースに対して、特定の操作を実行できるかを判断する仕組みです。

## 概要

あらゆる環境に万能な認可モデルはありません。実務上の課題は、組織の意味論を、スケーラビリティ、監査可能性、開発者体験を損なわずに適用可能な判断へ写像することです。

DAC や MAC のような古典的モデルも概念上は重要ですが、現代の企業システムでは RBAC、ABAC、ReBAC、ポリシー・アズ・コードの組み合わせが中心になります。最も堅牢な構成は、安定した粗粒度ロール、実行時の文脈評価、そして高感度または共有基盤向けの明示的なポリシーエンジンを重ねる形です。

## 主要概念

### 各モデルの概要

- **DAC（Discretionary Access Control / 裁量アクセス制御）**: リソース所有者が権限を付与する。協調型システムには向くが、中央統制には弱い
- **MAC（Mandatory Access Control / 強制アクセス制御）**: ラベルとクリアランスを中央で強制する。規制環境では強いが、変化の速い業務には硬直的
- **RBAC（Role-Based Access Control / ロールベースアクセス制御）**: 権限をロールに、ロールを主体に割り当てる。説明しやすいがロール爆発が起きやすい
- **ABAC（Attribute-Based Access Control / 属性ベースアクセス制御）**: 主体、リソース、操作、環境の属性を用いて判断する。柔軟だがポリシー肥大化の危険がある
- **ReBAC（Relationship-Based Access Control / 関係ベースアクセス制御）**: owner、viewer、approver、team member などの関係グラフで判断する。協調型・マルチテナント製品に強い
- **PBAC（Policy-Based Access Control / ポリシーベースアクセス制御）**: ポリシーロジックを専用システムや言語に切り出し、アプリコードから独立して評価する

### RBAC と ABAC の比較

RBAC は、責務が安定している場合に運用しやすい反面、例外のたびにロールを増やすと急速に脆くなります。ABAC は、地域、デバイス状態、取引金額、テナント分類、時間帯のような動的条件に対応できますが、属性とポリシーが増えるほど追跡と監査が難しくなります。

本質的な差は、静的か動的かではなく、管理の単純さと判断精度のどちらに寄せるかです。

### 権限付与項目と権能の違い

権限付与項目（Entitlement）と権能（Capability）は関係していますが、同じものではありません。

**権限付与項目** は、管理モデルの中で付与される許可の構造です。典型例として、ロール割当、グループ所属、ポリシーによる付与、常設権限があります。これは、ある主体が一般に何を許されているかをシステムに伝えるための仕組みです。権限付与項目は、組織構造、職務、所有関係、レビュー手順と整合しやすいため、持続的な運用モデルに向いています。

**権能** は、特定条件の下で特定操作を行うための境界付き権限です。通常は常設の権限付与項目よりも狭く、文脈依存で、委譲に向いています。権能はしばしば、リソース、時間制限、利用可能なツール、取引種別、タスク境界によって絞り込まれます。

実務上の違いは次のように表れます。

- **権限付与項目**: 「Alice は finance-approver ロールに所属している」
- **権能**: 「このエージェントは payment-review ツールのみを使って、今後 10 分間だけ請求書 12345 を承認できる」

この違いはアーキテクチャ上で重要です。

- 権限付与項目は、通常、管理、レビュー、長期的なガバナンスのために設計される
- 権能は、通常、実行、委譲、厳密な影響範囲制御のために設計される
- 権限付与項目は、主体が広く何を許されているかに答える
- 権能は、この主体が今この対象に対して、この制限の範囲で何をできるかに答える

多くのシステムでは両方が必要です。権限付与項目が、権能を要求または受け取るための基礎権限を定義し、その上で権能が実行時の実際の操作を絞り込みます。このパターンは、特権操作、ワークフロー承認、サービス間呼び出し、AI エージェントへの委譲で特に重要になります。

### ポリシーエンジンと評価フロー

現代のポリシーシステムは、しばしば次を分離します。

1. **ポリシーの作成**
2. **ポリシーの配布**
3. **判断の評価**
4. **適用**
5. **判断の記録と説明**

OPA、Cedar、Zanzibar 由来のグラフ型システム、XACML 系エンジンは、構文や配置形態は異なっても、同じ論点に直面します。すなわち遅延、キャッシュ、整合性、ポリシー展開の安全性、説明可能性です。

## 実装と運用

### 実務上の指針

- 安定した基礎権限には RBAC を使う
- 新しい恒久ロールを作るべきでない実行時条件には ABAC を重ねる
- 共有、委譲、協調グラフには ReBAC を使う
- ポリシーコードはアプリコードと同様にバージョン管理し、テストし、レビューする
- リスクが高い領域では、競合解決のために明示的な拒否の意味論を設計する

### スケーリング上の難所

大規模化すると難しいのは構文ではありません。データ鮮度、キャッシュ無効化、関係グラフの整合性、そして似たリクエストに異なる判断が出た理由を説明することです。

成熟度の良い指標は、認可ロジックをデプロイ前にテストできるか、インシデント分析時に過去の判断を再生できるかです。
